# Check Whether a URL is Reachable

Check Whether a URL is Reachable

## Usage

``` r
url_exists(url, timeout = 10)
```

## Arguments

- url:

  Character string. The URL to check.

- timeout:

  Numeric. Connection timeout in seconds. Default `10`.

## Value

`TRUE` if the URL responds, `FALSE` otherwise.
